---
import Layout from '../../layouts/Layout.astro';
import SiteHeader from '../../components/SiteHeader.astro';
import LearningMap from '../../components/LearningMap';
import { getCopy } from '../../utils/i18n';

const locale = 'zh-cn';
const copy = getCopy(locale);
const alternatePath = '/old/en/progress/';
---

<Layout
  title={`${copy.progress.title} | ${copy.brand.name}`}
  description={copy.seo.description}
  lang={locale}
  canonicalPath="/old/progress/"
  alternatePath={alternatePath}
>
  <main class="site-shell">
    <SiteHeader locale={locale} alternatePath={alternatePath} basePath="/old" />

    <section class="mx-auto mt-10 w-full max-w-6xl" data-reveal>
      <div class="rounded-[2.5rem] bg-gradient-to-br from-white/90 to-white/50 backdrop-blur-3xl border border-white shadow-xl px-8 py-14 text-[#1d1d1f] sm:px-12 sm:py-20 relative overflow-hidden transition-all duration-700 hover:shadow-2xl">
        <p class="soft-label mb-5">{copy.progress.kicker}</p>
        <h1 class="hero-title max-w-4xl font-semibold">{copy.progress.title}</h1>
        <p class="mt-5 max-w-3xl text-[17px] leading-[1.47] text-[#1d1d1f]/70">
          {copy.progress.intro}
        </p>
      </div>
    </section>

    <section class="mx-auto mb-14 mt-6 w-full max-w-6xl" data-reveal>
      <div class="panel overflow-hidden rounded-xl bg-[#f5f5f7]">
        <div class="relative w-full h-[70vh] sm:h-[78vh]">
          <LearningMap client:load />
        </div>
      </div>
    </section>

    <section class="mx-auto mb-24 w-full max-w-6xl" data-reveal>
      <div class="rounded-xl bg-[#f5f5f7] p-8 sm:p-12">
        <p class="section-kicker">Internship Goal</p>
        <h2 class="section-title mt-3">实习目标放在进度汇总端</h2>

        <div class="mt-8 border-t border-black/12">
          <article class="data-row md:grid-cols-[1.2fr_2fr]">
            <div class="font-semibold text-[#1d1d1f]">基础能力层（递进）</div>
            <div>由数学、编程与系统底层构成，作为后续所有能力的前置基础。</div>
          </article>
          <article class="data-row md:grid-cols-[1.2fr_2fr]">
            <div class="font-semibold text-[#1d1d1f]">专业能力层（并列）</div>
            <div>算法、系统、模型能力并列推进，形成可组合的工程与研究能力集合。</div>
          </article>
          <article class="data-row md:grid-cols-[1.2fr_2fr]">
            <div class="font-semibold text-[#1d1d1f]">科研实习汇总目标</div>
            <div>Google / Meta / Jane Street 等国际大厂实习作为终端汇总目标。</div>
          </article>
        </div>
      </div>
    </section>
  </main>
</Layout>
