---
import Layout from '../layouts/Layout.astro';
import SiteHeader from './SiteHeader.astro';
import { getCopy, localePath } from '../utils/i18n';
import LearningMap from './LearningMap';

const { locale, thoughts } = Astro.props;
const copy = getCopy(locale);
const alternatePath = locale === 'zh-cn' ? '/en/' : '/';
---

<Layout
    title={copy.seo.title}
    description={copy.seo.description}
    lang={locale}
    canonicalPath={localePath(locale)}
    alternatePath={alternatePath}
>
    <main class="site-shell">
        <SiteHeader locale={locale} alternatePath={alternatePath} />

        <section class="mt-12 sm:mt-16" data-reveal>
            <div class="panel-strong mx-auto max-w-5xl p-8 sm:p-12">
                <p class="soft-label mb-5">{copy.hero?.badge || 'HADMARD.GITHUB.IO'}</p>
                <h1 class="max-w-4xl text-4xl font-semibold tracking-tight text-slate-900 sm:text-6xl leading-tight">
                    {copy.hero?.title}
                </h1>
                <p class="mt-5 max-w-2xl text-base leading-8 text-slate-600 sm:text-lg">
                    {copy.hero?.description}
                </p>
                <div class="mt-8 flex flex-wrap items-center gap-3">
                    <a
                        href={locale === 'zh-cn' ? '/resume/' : '/en/resume/'}
                        class="inline-flex items-center rounded-full bg-slate-900 px-6 py-3 text-sm font-semibold text-white transition hover:bg-slate-800"
                    >
                        {copy.hero?.primaryCta || (locale === 'zh-cn' ? '查看简历' : 'View Resume')}
                    </a>
                    <span class="signal-pill">{locale === 'zh-cn' ? '简洁、持续、可读' : 'Simple, steady, readable'}</span>
                </div>
            </div>
        </section>

        <section class="mx-auto mt-16 mb-20 w-full max-w-5xl" data-reveal>
            <div class="mb-6 px-1">
                <h2 class="text-2xl font-semibold tracking-tight text-slate-900 sm:text-3xl">Path & Progress</h2>
                <p class="mt-2 text-sm text-slate-500 sm:text-base">
                    Fixed progression map split into clear sections, ending with a unified project row. Hover for quick tips, click for details.
                </p>
            </div>

            <div class="panel overflow-hidden rounded-3xl">
                <div class="relative w-full h-[70vh] sm:h-[78vh]">
                    <LearningMap client:load />
                </div>
            </div>
        </section>
    </main>
</Layout>
