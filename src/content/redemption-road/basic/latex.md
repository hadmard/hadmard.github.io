## 常用 LaTeX 数学命令表

### 1. 希腊字母

| 命令      | 效果      |
| --------- | --------- |
| `\alpha`  | $\alpha$  |
| `\beta`   | $\beta$   |
| `\gamma`  | $\gamma$  |
| `\delta`  | $\delta$  |
| `\theta`  | $\theta$  |
| `\lambda` | $\lambda$ |
| `\mu`     | $\mu$     |
| `\sigma`  | $\sigma$  |
| `\omega`  | $\omega$  |
| `\pi`     | $\pi$     |

大写：

| 命令      | 效果      |
| --------- | --------- |
| `\Gamma`  | $\Gamma$  |
| `\Delta`  | $\Delta$  |
| `\Theta`  | $\Theta$  |
| `\Lambda` | $\Lambda$ |
| `\Sigma`  | $\Sigma$  |
| `\Omega`  | $\Omega$  |

------

### 2. 上下标

```
x^2
```

$$
x^2
$$

```
x_i
```

$$
x_i
$$

```
x_{ij}
```

$$
x_{ij}
$$

```
x^{n+1}
```

$$
x^{n+1}
$$

------

### 3. 分数

```
\frac{a}{b}
```

$$
\frac{a}{b}
$$

------

### 4. 根号

```
\sqrt{x}
```

$$
\sqrt{x}
$$

```
\sqrt[n]{x}
```

$$
\sqrt[n]{x}
$$

------

### 5. 求和与积分

```
\sum_{i=1}^{n} i
```

$$
\sum_{i=1}^{n} i
$$

```
\int_a^b f(x)\,dx
```

$$
\int_a^b f(x)\,dx
$$

------

### 6. 极限

```
\lim_{x\to0}
```

$$
\lim_{x\to0}
$$

------

### 7. 常见运算符

| 命令      | 效果      |
| --------- | --------- |
| `\times`  | $\times$  |
| `\div`    | $\div$    |
| `\cdot`   | $\cdot$   |
| `\pm`     | $\pm$     |
| `\mp`     | $\mp$     |
| `\neq`    | $\neq$    |
| `\leq`    | $\leq$    |
| `\geq`    | $\geq$    |
| `\approx` | $\approx$ |
| `\equiv`  | $\equiv$  |

------

### 8. 集合符号

| 命令        | 效果        |
| ----------- | ----------- |
| `\in`       | $\in$       |
| `\notin`    | $\notin$    |
| `\subset`   | $\subset$   |
| `\subseteq` | $\subseteq$ |
| `\cup`      | $\cup$      |
| `\cap`      | $\cap$      |
| `\emptyset` | $\emptyset$ |

------

### 9. 常用箭头

| 命令              | 效果              |
| ----------------- | ----------------- |
| `\to`             | $\to$             |
| `\rightarrow`     | $\rightarrow$     |
| `\leftarrow`      | $\leftarrow$      |
| `\Rightarrow`     | $\Rightarrow$     |
| `\Leftrightarrow` | $\Leftrightarrow$ |

------

### 10. 括号自动伸缩

```
\left( \frac{a}{b} \right)
```

$$
\left(\frac{a}{b}\right)
$$

```
\left[ x^2 \right]
```

$$
\left[x^2\right]
$$

------

## Tex文件操作命令
