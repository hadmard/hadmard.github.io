### 按引用传递容器
传递容器一般使用地址而不是值，因为需要复制一份，占时间和内存
### 栈常见操作
- push
- pop
- peek
- <<打印栈，尽管原生cpp的stack不支持这个操作
- ==  和 != 来判定栈是否相同

### 栈常见应用
调用堆栈
互联网的后退

### 向量即栈
确实差不多？

### break

同python

### 队列
先进先出

### 队列的常见操作
- clear
- peek
- dequeue
- enqueue
- equals
- isEmpty
```cpp
while (!q.isEmpty())
{
	cout << q.dequeue() << endl;
}
```

### 后缀表示法
