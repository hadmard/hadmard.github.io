首先一次MoE的前向过程中，Token为FP32
先选专家，这里是使用FP32
这里可以用AMX，不同的token拼在一起计算（这里不能用，因为AMX处理的INT8，那还是使用AVX-512）
算完每个token选自己的专家
然后重排每个专家的token，以及共享专家
这里是要先量化为INT8，然后每个都是要做两个上采样和一个下采样，这里可以拼接使用AMX来计算
路由权重就是上面算的吧，然后累加，这里处理的都是向量，那就使用AVX-512
处理完之后反量化为FP32
